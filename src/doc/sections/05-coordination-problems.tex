\section{Synchronization and Coordination Problems}
\label{sec:problems}

\stub{2.5 pages. The problems only, no solutions: those are \cref{sec:addressing}
and \cref{sec:fault-tolerance}. One paragraph per problem P1--P7, saying where it
arises, why it is hard, and what would happen without a solution. The division
that must come across is between problems inside one station (P1, P5, P6) and
problems across nodes (P2, P2b, P4). Close on the communication problems:
heterogeneous runtimes exchanging structured data, long-lived low-latency
connections to many clients, asynchronous state propagation without polling.
Source: SCOPE §5, DESIGN-NOTES §2.}

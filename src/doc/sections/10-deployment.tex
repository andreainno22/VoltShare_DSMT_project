\section{Deployment}
\label{sec:deployment}

\stub{2 pages. Seven nodes, the containers, the networks, how the system is
started. Taken from deploy/docker-compose.yml, not from memory. State precisely
that the deployment runs on one host with seven Erlang and JVM nodes, and that a
genuine network partition is produced with \texttt{docker network disconnect}
rather than with a second machine. Include the per-site networks, which exist to
make a site partition demonstrable. Sources: deploy/, DEMO.md.}

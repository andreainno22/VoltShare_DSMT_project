\section{Testing and Demonstration}
\label{sec:testing}

\stub{2 pages. What is tested automatically (EUnit, scripts/eunit\_check.sh, with
the expected test count) and what is shown live. The scenarios are in DEMO.md:
contention on one connector, cross-node claim, no-show, power sharing in real
time, coordinator failover, station crash, station partition. For each one: what
appears on screen and which problem of \cref{sec:problems} it demonstrates. That
mapping is what turns a demo into evidence.}

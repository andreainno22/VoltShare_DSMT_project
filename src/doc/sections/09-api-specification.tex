\section{API and Protocol Specification}
\label{sec:api}

\stub{4 pages. One message table per channel: name, direction, payload, effect,
errors. A table beats a listing here, it fits in less space and can be consulted.
9.1 HTTP endpoints of the back office, with the JWT note (contracts/jwt.md);
9.2 driver WebSocket (contracts/ws-driver.md); 9.3 charge-point WebSocket
(contracts/ws-chargepoint.md), declaring which OCPP 1.6-J messages are
implemented and which are not; 9.4 claim protocol (contracts/claim.md);
9.5 JInterface bridge (contracts/erlang-java.md). One sequence figure for the
reservation with claim, including the refusal path.}
